# Substitution ledger

Every place where the Go code replaces Rust code or a Rust crate with a
different library, or with a port whose semantics could drift, has a row
here. A pull request that adds a dependency or swaps an implementation
without a row is not merged (plan §3, principle 3: a substitution is a
behaviour change until proven otherwise).

| Area | Rust | Go | Known differences and how they are closed | Pinned by |
|---|---|---|---|---|
| Semver ranges (image policies) | `semver` crate `VersionReq` | Port of the crate's range grammar, `Display` and matching (`core/imagepolicy/req.go`); `Masterminds/semver/v3` only parses tag versions (`StrictNewVersion`) | Masterminds reads bare `1.2.3` as exact (Rust: caret), treats spaces as AND, has `\|\|`, matches pre-releases more widely → not used for ranges. Masterminds rejects versions over 256 bytes; OCI tags are at most 128. | `imagepolicy_test.go`, 2 fuzz targets |
| Tag globs | hand-written matcher | byte-for-byte port | `path.Match` not used (it treats `/`, `?` and classes specially) | `imagepolicy_test.go` |
| Domain names (IDNA) | `idna::domain_to_ascii` | `golang.org/x/net/idna` custom profile: MapForLookup, BidiRule, CheckJoiners, no hyphen/STD3/DNS-length checks, then the Rust LDH and length checks | `idna.Lookup` rejects `ab--cd.com` and applies STD3 → not used. Unicode table versions differ (x/net: Unicode 17): a newly assigned code point could map differently; x/net is pinned and existing domains are checked before cutover C1. | `domain_test.go` (+ quick property) |
| Advisory lock keys | `domain::lock_key` | same bytes | none | `domain_test.go` |
| Timestamps from GitHub | `jiff::Timestamp::from_str` | `time.Parse(time.RFC3339Nano)` | Go rejects lowercase `t`/`z`, space separator, `:60`, RFC 9557 suffixes; GitHub sends none. Pre-1970 fractions floor instead of truncate. | `source_test.go` |
| Configuration | `figment` (defaults → /etc/kuben/config.toml → ./kuben.toml searched upward → `KUBEN_*` with `__`) | `koanf/v2` + a port of figment's env-value grammar + own nesting (koanf's `maps.Unflatten` clobbers tables) + strict mapstructure decoding | Decode error texts differ. A bare number/boolean for a text setting from env is accepted as text (figment refused it unquoted) — accepted by the owner 2026-09-21. | `config` tests, `FuzzEnvValue` |
| Secrets in configuration | plain `String` | `config.Secret` (redacted in fmt/%v/%+v/%#v/%d/JSON) | `sso.client_secret = ""` means unset — accepted 2026-09-21. | `config` tests |
| Canonical JSON and content hashes | `render::canonical` over `serde_json::Value` | `wire.Canonical`: byte-sorted keys; serde's string escaping (not DEL, U+2028/2029, `<>&`); serde's float printing (plain for exponents −5…15, else `1e+21`/`1e-6`); a port of serde_json's number *parser*, which is not correctly rounded (`strconv.ParseFloat` differs on 3 of 18 pinned texts), with the literal 1e0…1e308 table (`math.Pow10` differs at 78 exponents) | none known; canonical text with floats is not a fixed point, in Rust either | `testdata/compat/{canonical,floats}.json` from Rust (6 documents, 4000 floats, 18 number texts); fuzz |
| Other JSON output | `serde_json` | `encoding/json` with `SetEscapeHTML(false)` where bytes are a contract (webhook bodies, stored JSON) | `encoding/json` escapes `<`, `>`, `&` and U+2028/2029 by default | per package |
| Rust `{:?}` in messages | Debug formatting | `%q` / local `rustQuote` | differs only for control and grapheme-extend characters | package tests |
| HTTP API server | axum + utoipa (code first) | ogen v1.24 (spec first, server only) from the unchanged `openapi.json` | Generated `Opt*`/`OptNil*` DTOs instead of domain types: converted at the route layer. The two SSE endpoints are hand-written handlers. ogen validates requests from the spec (enums, formats, required members). A gate checks the principal before decoding, as axum's extractor order did; a malformed request from a signed-in caller answers 422 validation_failed (axum: plain-text 400 for query rejections). | `go/SPIKES.md`; route tests (S1+) |
| Agent kind→resource mapping | kube-rs discovery | client-go discovery + `restmapper.NewDeferredDiscoveryRESTMapper` over a memory cache | Same answers for served resources; the mapper is reset when a kind is not found (CRDs installed later). | agent tests (S2) |
| Schema migrations and their ledger | `sqlx::migrate!` (sqlx-core/sqlx-postgres 0.9) on `_sqlx_migrations` | `store/migrate`: a port of sqlx's resolver and Postgres migrator on the same table, rows and advisory lock; `store/migrations` embeds byte-identical copies of the 33 files | Same: checksum = SHA-384 of the file bytes (sqlx strips nothing without a sqlx.toml, and Kuben has none); name split at the first `_`, i64 version, description = rest minus the `.sql`/`.up.sql`/`.down.sql` suffix (repeatedly, `trim_end_matches`) with `_` → space; `-- no-transaction` prefix; lock id `0x3d32ad9e × crc32_ieee(current_database())`; lock → ensure table → dirty check → missing check → per migration: checksum compare or apply (SQL by simple protocol + ledger row with `execution_time = -1` in one transaction, then `UPDATE … execution_time = ns`) → unlock. Differences: after a failed run sqlx leaves the session lock on a pooled connection; the Go store closes that connection. `undo`, `skip`, `run_to`, `ignore_missing` and schema creation are not ported (Kuben never used them). Database error texts inside `while executing migration N: …` are pgx's, not sqlx's. | `migrate_test.go` (pinned checksums from `shasum -a 384`, lock ids from zlib.crc32, name rules, ledger rows and refusals against PostgreSQL), `migrations/embed_test.go` (byte-identical to the Rust files while they exist) |
| PostgreSQL driver and pool | sqlx `PgPool` (typed binds, prepared-statement cache, `acquire_timeout(10 s)`, `max_connections(max(n, 2))`, idle 10 min, lifetime 30 min) | pgx v5 `pgxpool` behind `store.pool`, which bounds every acquisition by 10 s (pgxpool has no acquire timeout) | sqlx sent a type with every bind; pgx lets the server infer parameter types, so `INSERT … SELECT $n` relies on PostgreSQL coercing the unknown parameter to the column type (true since PG 10, not exercised in the sandbox; CI runs every statement). URL parsing is pgx's `ParseConfig` (libpq rules; sqlx's own parser accepted the same URLs Kuben documents). Database error texts differ (`database error: <op>: ERROR: … (SQLSTATE …)`). JSON written to text/jsonb columns goes through `wire.CanonicalValue`, serde_json's `Value::to_string`; content hashes (`releases.content_hash`, `render_plans.content_digest`, `target_config_revisions.config_hash`) are taken over that text, so a JSON float must reach the store as `json.Number` to keep serde's `1.0` (a `float64` prints `1`). Where the server would infer another type than sqlx bound, because a parameter's first use is a cast or a comparison, the Go SQL casts it where it is stored, to the type Rust bound: `deployments.go` INSERT_RUN `$1::uuid, $5::uuid, $6::uuid, $7::uuid, $9::bigint, $10::uuid` (first used in `::text` for the approval plan hash) and `$15::smallint` (first used in `$15 > 0`, an integer). Rust tuples in JSON output (`SupportSummary`) are arrays through `MarshalJSON`. | store tests against PostgreSQL 17 in CI; `TestIDsEncodeAndScanForBothColumnTypes` (ids through pgx in text and uuid columns, no database) |
| CRD types (kuben.dev/v1alpha1) | `serde` derives + kube-rs `CustomResource`/`KubeSchema` (`validate_cel` in tests) | `kubenapi/v1alpha1`: Kubernetes-style types, serde defaults and skip rules reproduced by `UnmarshalJSON`/`MarshalJSON`, enums refuse unknown strings; DeepCopy by controller-gen; the tests validate with the apiextensions-apiserver OpenAPI and CEL validators over the frozen manifest | Go is more lenient on decode: a missing required member, `null` for a non-nullable one and duplicate keys are accepted (serde refused them; the apiserver's schema refuses the first two). Types with a custom decoder match member names case-insensitively (`encoding/json`), serde and `sigs.k8s.io/json` case-sensitively. Unsigned widths kept (`uint16` ports, `uint32` counts) to match the manifest's formats. Zero enums encode as the Rust `#[default]` variant. | `objects_test.go` (34 fixtures against the Rust oracle `testdata/rust-oracle.rs.txt`), `schema_test.go` |
| Kubernetes watches for the projections | kube-rs `runtime::watcher` (+ `default_backoff`, `.modify` clearing managedFields), `Init/InitApply/InitDone` staging, `tokio::sync::broadcast(1024)` fan-out | client-go `SharedInformerFactory` (Pods, label `app.kubernetes.io/managed-by=kuben`) and dynamic informers (Projects, Environments, Apps; HTTPRoutes with the same selector; cert-manager Certificates filtered to `kuben-tls-*`) with a transform that drops managedFields; `platform/projection` | The first LIST is swapped in once the informer reports synced (adds of the initial list are ignored, so readers never see a half set). A later re-list arrives as individual upserts and deletes (client-go's Replace), not as a `resync` delta; the Rust watcher emitted `resync` on every re-list. kuben.dev objects are decoded through their `UnmarshalJSON` (serde defaults); an undecodable one is skipped with a warning. Fan-out: one bounded queue (1024) per subscriber; on overflow the backlog is dropped and the next receive is a lag notice with the missed count (Rust: `Lagged(n)` then the oldest retained deltas). Views and deltas are encoded by `encoding/json` without HTML escaping at the top level, but `opt.Val` members still escape `<`, `>`, `&` (`\u003c`) and U+2028/2029, and `stream.Serve` HTML-escapes the snapshot: the same JSON values, not always the same bytes. | `projection` tests (fake clientset, fake dynamic client), `TestDeltasSerializeAsTheRustEnum`, `TestViewsSerializeAsTheRustStructs` |
| Render plan objects | k8s-openapi typed objects serialized by serde (unset `Option`s absent) | JSON maps built field by field in `platform/render` (typed client-go objects would add `creationTimestamp: null`, `status: {}`, `strategy: {}` …) | none known: the three Rust golden snapshots are compared byte for byte, and the canonical text goes through `wire.CanonicalValue` | `render` golden tests (`testdata/*.snap`) |
| Controller leader election | hand-written Lease loop (`leader.rs`: CAS on resourceVersion, expiry by local observation) | client-go `tools/leaderelection` with a `LeaseLock`; `platform/leader` | Same Lease (`kuben-controller`), same 15 s / 10 s / 2 s, same identity (`$HOSTNAME` or `kuben`, `_`, 8 hex digits), same fields (`acquireTime`, `leaseTransitions` 0 on create and +1 on take-over), released on shutdown with `leaseDurationSeconds: 1` (`ReleaseOnCancel`). On release client-go writes `holderIdentity: ""` where Rust removed the member; both read as "no holder" to either implementation, so a mixed Rust/Go rollout (cutover C2) hands over correctly. | `leader_test.go` (fake clientset, two replicas) |
| Cluster capability discovery | kube-rs `list_api_groups`, typed and dynamic `Api::list`, tokio `watch::channel(None)` for the facts, `Instant` for the rewrite interval | client-go discovery (`ToDiscoveryInterfaceWithContext`), typed clientset and dynamic client; `discovery.Watch` (mutex + a channel closed on change) in place of the watch channel; `platform/discovery` | A round cancelled mid-discovery publishes and records nothing (Rust finished the round: its token only interrupted the sleep); the probes it cancelled would have reported every fact unknown. "Changed" is `ClusterFacts.Equal`, which ignores empty versus absent lists and the order of served kinds (Rust: derived `PartialEq`), so an equivalent answer is not republished. The ten-minute rewrite is measured on the injected clock (Rust: monotonic `Instant`). A nil list or discovery client with no error is a failed probe. | `discovery` tests (fake clientset, fake discovery, fake dynamic client; `TestWireFormIsPinned`, `TestDecodingIsAsStrictAsSerde`) |
| Materializer writes | kube-rs `Api<K>` over the typed kuben.dev objects (serde JSON), `PatchParams::apply("kuben-materializer").force()` carrying the read `resourceVersion`, `PostParams` creates; a `watcher(...).touched_objects()` on the managed Apps for drift | client-go dynamic client over the serde-compatible JSON of the kubenapi types (`platform/materializer/kube.go`): `Apply` with `FieldManager: kuben-materializer, Force: true` and the read `resourceVersion`, `Create` with the same field manager; a dynamic informer on the managed Apps whose add/update handler checks drift | The applied body is the same JSON, except that `metadata.creationTimestamp: null` (which `metav1.ObjectMeta` writes) is removed so the field sets match. A 409 is recognised by status code, as Rust did (`AlreadyExists` included). The informer re-lists with client-go's backoff instead of kube-rs `default_backoff`; like the Rust watcher it sees adds and updates, not deletes. A config revision is decoded by `v1alpha1.DecodeAppSpec`: missing or null required members are refused as serde refused them; the error texts are Go's (the code, `InvalidConfig`, is the same). | `materializer` tests; tests/materializer.rs with envtest in CI |
| Reconcilers (controllers) | kube-rs `runtime::Controller` (reflector stores, `owns`/`watches`/`reconcile_all_on`, per-object failure map with `backoff`, `Action::requeue`) | controller-runtime v0.25.1 manager and builder in `platform/controller`; the gateway loop is a manager Runnable; `workqueue.NewTypedItemExponentialFailureRateLimiter(2 s, 5 min)` | Same delays (2 s, 4 s, 8 s … 5 min) and reset on success. The manager is started without leader election (platform/leader elects), without metrics/health listeners, with managedFields stripped from the cache and Namespaces/Deployments/Services cached only with `managed-by=kuben`. Live reads stay live (API reader). A kind the server does not serve (`NoKindMatchError`) counts as a 404. The KubenConfig singleton is chosen in name order (Rust: store order, arbitrary). KubenConfig deletions also re-reconcile Apps (Rust: applied objects only). 4 workers per controller (kube-rs: unbounded; one object is never reconciled twice at once in either). `kuben_reconcile_errors_total` is not emitted yet (no metrics registry in the hub); controller-runtime also logs its own "Reconciler error". Gateway write failures carry client-go's error text. controller-runtime's process-wide logger is set once by serve. | `controller` tests (controller-runtime fake client; `TestTheRateLimiterWaitsTheBackoff`); envtest in CI |
| Response compression | tower-http 0.7 `CompressionLayer` (br, zstd, gzip, deflate) with `DefaultPredicate` | `httpx.Compressor`: gzip from the standard library with the same predicate (≥ 32 bytes, not images but SVG, not `text/event-stream`, not gRPC but gRPC-web, not when `Content-Encoding` or `Content-Range` is set), `Vary: Accept-Encoding`, `Content-Length`/`Accept-Ranges` dropped | No brotli, zstd or deflate (not in the standard library; plan §21 puts brotli at build time for the console's static files): a client asking `br` gets gzip. Body size is judged by the first 32 bytes written, not a size hint. A client refusing both gzip and identity gets the plain body, not 406. The event streams that write to the outer writer (`logs?follow`) are uncompressed, as SSE was in Rust. | `httpx/compress_test.go` |
| Outbound HTTP | `transport.rs`: hyper + hyper-rustls (native roots), hyper-util proxy matcher from env, tokio timeouts | `integrations/outbound`: net/http, `ProxyFromEnvironment`, a timer until headers and another for the body, `io.LimitReader` | Go's proxy rules (`HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY`, CGI guard) match hyper-util's for the variables Kuben documents; error texts of failed connections are Go's; the https-only refusal says "invalid URL, scheme is not https" before any connection (hyper-rustls refused at connect). | `outbound_test.go` |
| Doctor port probe | tokio `TcpStream::connect` with a 3 s timeout | `net.Dialer` through `doctor.Dialer`, context timeout 3 s | same `{ip}:{port}: …` text; the reason is the system's ("connection refused"), not io::Error's "Connection refused (os error 111)" | `checks_test.go` |
| Kubernetes API in tests | a kind cluster for `#[ignore]` tests | envtest's kube-apiserver and etcd (`platform/kubetest`) | no controllers: no garbage collection, namespaces stay Terminating, nothing becomes ready; the tests play the controllers as the Rust ones did | `kubetest`, `controller`, `materializer` cluster tests |
| OCI image resolution and tag listing | hand-written OCI distribution client over hyper (`oci.rs`: challenge parser, token exchange, HEAD→GET, Link pagination) | `github.com/google/go-containerregistry` v0.22.1 `remote.Puller` (Head, Get, Lister) and `authn`; the grammar of references is ported byte for byte (`integrations/oci/parse.go`), not ggcr's `name` parser | Adds a `GET /v2/` ping; Docker Hub via `index.docker.io`; 10 s per resolution / per tag page (Rust: per request); ggcr retries turned off; the 4 MiB body cap kept by a transport wrapper (ggcr's 64 KiB token/error caps also apply); GET fallback also when HEAD lacks Content-Type/Length, and GET hashes the body (Rust: header first); schema1 accepted; realm/redirect SSRF guards (a private-IP realm on another host → Unauthorized); Link: first `<…>`, absolute same-host URLs followed, a malformed header fails the listing; 429 on resolve is `RateLimited` (Rust: Unreachable "HTTP 429") and the app routes answer 503 for both, as Rust did, with the clearer text; network error texts are Go's; ALL_PROXY not honoured, HTTP/2 possible; plain HTTP refused by a transport wrapper (ggcr would use it for localhost/RFC 1918). Pulls docker/cli config + logrus into the binary. `Login` never prints or serializes its password. | `integrations/oci` tests against ggcr `pkg/registry` on httptest's in-memory TLS network |
| Usage metrics | kube `Api<DynamicObject>` on the PodMetrics kind | client-go `dynamic.Interface` on `metrics.k8s.io/v1beta1` `pods` (`platform/usage`); k8s.io/metrics is not added | The "unavailable" reason's text comes from client-go's error, not kube-rs's display. | `TestRunScrapesAndRollsUp`, `TestPodMetricsAreSummedPerApp` |
| GitHub App client | hand-written hyper client, ring RSA/HMAC, moka cache | go-github v92 builds and sends requests (rate-limit tracking off) through `integrations/outbound` (https only unless the API URL is `http://`, proxy from env, 15 s headers + 15 s body, 1 MiB cap; Accept/Content-Type as Rust); Rust's path encoding and field checks; JWT by crypto/rsa, webhook HMAC by crypto/hmac, the token cache by golang-lru `expirable`; ghinstallation is **not** used (decided by the owner, 2026-09: the hand-written cache stays; its JWT is backdated 30 s and lives 2 min, Rust 60 s / 9 min; one token per transport refreshed 1 min early, Rust a cache per installation and repository, 50 min, dropped 10 min before expiry; different errors; golang-jwt and a second go-github) | JSON body members in another order with a trailing newline; error details differ (no serde line/column, Go's key-parse and file texts, Go's reason phrases such as "Request Entity Too Large"); duplicate JSON keys: the last wins (serde refused); the 50-minute TTL runs on wall time, the 10-minute margin on the injected clock. | `integrations/github` tests (fake GitHub, JWT verified with the public key, exact claim bytes) |
| OIDC / JWT (CI trust) | ring `RSA_PKCS1_2048_8192_SHA256`, base64 crate, hyper HTTPS GET, `Instant` | crypto/rsa `VerifyPKCS1v15`, encoding/base64 (RawURL, Strict), `integrations/outbound`, `clock.Clock` (`integrations/oidc`); no JWT library | ring's key rules kept (2048–8192 bits, no leading zeros, odd e ≥ 3) but e above 2^31−1 is refused (ring allowed up to 2^33−1); CR/LF in a part is refused explicitly (Go's decoder skips it); a bad JWKS's error text is encoding/json's. | `oidc_test.go` |
| SQL assembled with `format!` | sqlx `AssertSqlSafe(format!(…))` over module constants | Go constant string expressions with the same text | none: the compiler proves the text is constant | `store` builds tests |
| Secrets at rest | `ring` AES_256_GCM + `zeroize` | `crypto/aes` + `crypto/cipher` GCM, `crypto/rand` (`platform/secrets`) | Same AAD, layout (nonce‖ciphertext‖tag), fingerprint and plaintext (serde's BTreeMap JSON via `wire.CanonicalValue`). Keys are not zeroized on drop (data keys and file text are cleared after use). I/O error texts inside KeyringError are Go's. Base64 is `StdEncoding.Strict()` with CR/LF refused, as the base64 crate. | `platform/secrets` tests, `testdata/compat/secrets.json` (sealed by Rust) |
| Revision Secret writes | kube-rs `Api<Secret>` create with a field manager | client-go `CoreV1().Secrets` Create with the same FieldManager | none known | `materializer/secrets_test.go` |
| AgentLink TLS | rustls (ring), TLS 1.3 | crypto/tls, TLS 1.3 (`kubenapi/protocol`) | The agent always sends its certificate (crypto/tls would silently send none when the CA hints differ); Go also offers X25519MLKEM768 where rustls picks X25519; server-name rules ported from pki-types. | `tls_test.go`, `mtls_test.go`, `link_test.go` |
| Agent certificates | rcgen 0.14 | crypto/x509 | Same P-256 keys, validity, serials, key identifiers and UTF8String names; only the extension order differs. | `TestTheCertificatesHaveTheShapeRcgenGaveThem` |
| AgentLink JSON | serde_json derives | hand-written encoder and decoder | The same bytes by construction; decoding accepts duplicate keys (the last wins) and not serde's array form of a struct. | `TestEveryMessageEncodesAsSerdeDid` |
| Agent apply | kube-rs `Patch::Apply` | dynamic client `Patch(ApplyPatchType)` on the raw JSON with fixed plurals | Error texts are client-go's. | agent runtime tests (kind) |
| Agent CLI and logs | clap, tracing JSON | cobra with its own env and conflict rules, slog JSON to stdout | Message texts and log field names differ; `RUST_LOG` is read only as a plain level. | `args_test.go` |
| Registry digest verification (builds) | `oci.rs` RegistryVerifier: hand-written HEAD with token exchange | go-containerregistry `remote.Puller.Head` through the resolver's session (`name.Insecure` for `build.insecure_registry`) | An insecure registry is pinged over HTTPS first, then HTTP (Rust went straight to http://); a 200 still decides by the `Docker-Content-Digest` header alone. | `integrations/oci/verifier_test.go` (in-process registry) |
| Webhook targets and delivery errors | `http::Uri`, tokio `lookup_host`, hyper's error chain | `net/url`, `net.Resolver.LookupNetIP`, net/http errors (`notify`) | Same rules and texts for scheme, host and private-address refusals and for `HTTP <code> <reason>` (the http crate's phrases are kept in `notify.CanonicalReason`, since `http.StatusText` words 203, 413, 414 and 416 differently); the URL-parse and resolver details and the network error texts stored as `last_error` are Go's. | `notify` tests |
| Free-form object responses | serde_json maps (sorted keys) | ogen map types for `{type: object}` members | Nested values go through `wire.CanonicalValue` and keep serde's sorted keys and number printing; the top-level members of a free-form object (the export document) come out in Go map order. Same JSON value, not the same bytes. | `apps_export_test.go` |
| Logs and metrics | tracing-subscriber `EnvFilter` (RUST_LOG, per-target directives), `metrics` + metrics-exporter-prometheus | `log/slog` JSON/text; `prometheus/client_golang` with one registry per process hung on `health.Health` (`platform/metrics`) | Only the global level of a directive list counts (`debug,hyper=info` is debug for everything); log field names and message texts are slog's. The exporter answers the text format on any path of `server.metrics_bind` like Rust's; client_golang adds HELP/TYPE lines and no `go_*`/`process_*` collectors are registered. | `metrics_test.go`, `logger_test.go` |
| CLI terminal input | `stty -echo` around reading the token, `IsTerminal` | `golang.org/x/term` (`ReadPassword`, `IsTerminal`) | Same behaviour on Unix terminals; works on Windows consoles too. | `cli` client tests |
| CLI's HTTP client | kuben-api `transport.rs` (hyper, rustls native roots, proxy from env) | net/http `DefaultTransport` (`apiclient`) | Proxy from the environment and the system roots as before; a plain-http proxy is used directly instead of through CONNECT; connection error texts are Go's. A required JSON member that is missing decodes as its zero value (serde refused), except in problem documents. `apps --json` escapes U+2028/2029; `status --json` goes through `wire.Canonical`. | `apiclient` tests |
| YAML of `kuben dns01-issuer --dry-run` | serde_yaml_ng | `sigs.k8s.io/yaml` (JSON → YAML, sorted keys) | Compared with the v1.2.0 binary (default and `--staging` output byte-identical). serde_yaml_ng left YAML 1.1 booleans (`y`, `on`, `no`) and timestamps plain and single-quoted `true`/`null`/numbers; sigs.k8s.io/yaml double-quotes all of them — kept, since kubectl reads YAML 1.1. clap refused a value starting with `-` (`--namespace -1`); pflag takes it. | `TestTheDryRunPrintsBothObjects`, `TestAValueThatReadsAsABooleanIsQuoted` |
| Support bundle cluster section | kube-rs typed objects | client-go typed objects | Node quantities print in client-go's canonical form; event `count` 0, `unschedulable` false and empty reason/phase are written as null; KubenConfig status is the API server's raw object. | `cli/support` tests |
