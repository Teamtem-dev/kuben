{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "gen": {
      "inputs": ["openapi.json"],
      "outputs": ["src/schema.d.ts"]
    },
    "check": {
      "inputs": ["$TURBO_DEFAULT$"]
    }
  }
}
