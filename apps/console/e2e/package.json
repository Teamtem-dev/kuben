{
  "name": "@kuben/console-e2e",
  "private": true,
  "description": "Browser tests of the console (Playwright, axe). Not a workspace: CI installs these exact versions next to the tests.",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0"
  }
}
