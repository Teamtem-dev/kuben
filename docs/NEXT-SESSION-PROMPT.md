Continue Kuben's Go rewrite on branch `feat/go-rewrite` (repository Teamtem-dev/kuben). Reply to me in Persian; code, comments and commits in English. Commit and push to feat/go-rewrite only; no PRs, no tags.

1. `git fetch origin feat/go-rewrite && git checkout feat/go-rewrite`. Read, in order: docs/HANDOFF-GO.md (§0 rules, §2b cloud environment, §7 verification, §8 status), go/CONVENTIONS.md, docs/GO-REWRITE-ROADMAP.md, docs/KUBEN-GO-REWRITE-PLAN.md §5–§6, go/PARITY.md, go/SUBSTITUTIONS.md. The Rust crates under crates/ are the frozen reference: reproduce JSON names, null vs omitted (ogen OptNil → SetToNull/optNil*), error texts ("validation failed: …", "conflict: …", "not found: …"), status codes, SQL verbatim and audit strings, and port every Rust test (standard library + go-cmp only).
2. Environment: start PostgreSQL (`service postgresql start`; URL postgres://postgres:kuben@localhost:5432/<private db>, `KUBEN_REQUIRE_PG=1`), install envtest (`go run sigs.k8s.io/controller-runtime/tools/setup-envtest@v0.25.1 use 1.37.x -p path`, `KUBEN_REQUIRE_K8S=1`), golangci-lint v2.13.2. Never run `go mod tidy`/`go get` except for a library plan §5 names; restore go.work.sum before committing; docs/ is gitignored (`git add -f`).
3. First finish the work in progress in docs/go-wip/*.patch, one at a time: `git apply --3way docs/go-wip/<name>.patch`, resolve (keep both sides in api/server.go and serve/serve.go), read it against its Rust files (listed in HANDOFF §8), complete it, port all its tests and http.rs scenarios. Then continue with the "Still to do" list in HANDOFF §8: evidence route + doctor wiring, S5 previews/client/host, the partials, G5 (CLI, bootstrap, bundle, telemetry), G6 (release train), G7 (remove Rust).
4. You may run up to 4–5 parallel agents in git worktrees, each on non-overlapping files with a private test database; give them the rules above. Cherry-pick their commits onto feat/go-rewrite yourself.
5. Before every push, from go/hub (and go/agent, go/kubenapi when touched): `go build ./... && go vet ./... && test -z "$(go tool gofumpt -l .)"`, the full `go test -race ./...` with PostgreSQL and envtest, `bash ../../scripts/go-check.sh`, and golangci-lint with 0 issues. Commits: Conventional Commits, one unit per commit, body says why.
6. After each unit, update go/PARITY.md (row + recomputed totals) and go/SUBSTITUTIONS.md; at the end of each phase update docs/HANDOFF-GO.md §8 (tick what is done) and give me a short Persian report.
