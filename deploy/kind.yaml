# Local cluster for development and the e2e suite:
#   kind create cluster --config deploy/kind.yaml && bun run e2e
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kuben
nodes:
  - role: control-plane
