// Package tools pins the versions of the developer tools the Go code is
// built and checked with (`go tool <name>`), in a module of its own so that
// their dependencies never reach the hub or the agent.
package tools
